machine:
  ghc:
    version: 7.10.1
dependencies:
  pre:
    - cabal build
  post:
    - sudo pip install awscli
test:
  cache_directory:
    - _site
  override:
    - dist/build/expoundite/expoundite build
    - ./retouch.sh
deployment:
  aws:
    branch: master
    commands:
      - ./publish.sh