machine:
  ghc:
    version: 7.10.1
dependencies:
  cache_directory:
    - _site
  pre:
    - cabal build
  post:
    - sudo pip install awscli
test:
  override:
    - dist/build/expoundite/expoundite build
    - ./retouch.sh
deployment:
  aws:
    branch: master
    commands:
      - ./publish.sh